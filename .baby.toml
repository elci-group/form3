schema = "baby.install/v1"
build_system = "cargo"
binary = "3form-agent"
artifact = "target/release/3form-agent"
commands = [["cargo", "build", "--release", "--bin", "3form-agent", "--features", "agent"]]
